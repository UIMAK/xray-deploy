{
    "listen": "{{LISTEN}}",
    "port": {{PORT}},
    "protocol": "vless",
    "tag": "{{TAG}}",
    "settings": {
        "clients": [
            {
                "id": "{{UUID}}"
            }
        ],
        "decryption": "{{DECRYPTION}}"
    },
    "streamSettings": {
        "network": "xhttp",
        "xhttpSettings": {
            "path": "{{PATH}}"
        },
        "security": "reality",
        "realitySettings": {
            "target": "{{TARGET}}:443",
            "serverNames": [
                "{{SERVER_NAME}}"
            ],
            "privateKey": "{{PRIVATE_KEY}}",
            "shortIds": [
                "{{SHORT_ID}}"
            ],
            "minClientVer": "1.0.0"{{MLDSA65_SEED_BLOCK}}
        }
    },
    "sniffing": {
        "enabled": true,
        "destOverride": ["http", "tls", "quic"],
        "routeOnly": true
    }
}
